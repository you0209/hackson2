\section{担当B — センサ処理・状態機械}

\subsection{機能一覧}

担当Bでは，楽器ユニットにおけるセンサー処理およびArduinoによる制御を担当する．
本システムでは，指揮者ロボットの腕の動きをセンサーによって観測し，
その情報をもとに演奏タイミングを決定する．

各楽器ユニットは独立して動作しており，
通信を用いることなく指揮者の動きに追従して同期演奏を行う．
担当Bはその中核として，動きの検出とタイミング推定を担う．

各楽器ユニットは，あらかじめ内部に楽譜データを保持しており，
取得した拍情報に基づいて順次演奏を行う．
また，各楽器の演奏開始タイミングは，親機からのカラーキューによって動的に指示される．カラーキューの判別にはカラーセンサを用い，フォトトランジスタは拍タイミング検出に引き続き使用する．
演奏者がProcessing GUIから任意の順序で各楽器を指定することで，輪唱のような演奏を実現する．

担当Bが提供する主な機能を以下に示す．

\begin{itemize}
  \item フォトトランジスタによる拍タイミングの検出
  \item 赤外線センサによる腕の位置・距離の計測
  \item 移動平均および正規化による距離データの平滑化
  \item EMA（指数移動平均）によるテンポ推定
  \item 赤外線センサによる次拍時刻の予測
  \item テンポ変化の検出と追従
  \item 腕の静止判定によるフェルマータ検出
  \item 状態機械による演奏状態の管理
  \item 担当Cへの拍タイミング・状態情報の共有
\end{itemize}

\subsection{システム構成図}

図\ref{fig:system} にシステム全体の構成を示す．

\begin{figure}[H]
\centering
\includegraphics[width=0.8\linewidth]{figures/tantoB/system_fig_B.pdf}
\caption{システム構成図}
\label{fig:system}
\end{figure}

\subsection{操作インターフェース設計}

本システムでは，指揮者ロボットの腕の動きが操作インターフェースとなる．
各楽器ユニットはセンサーを通じて指揮者の動きを観測することで，
ユーザーが個別の楽器を操作することなく演奏タイミングを制御できる．

センサによるテンポ計測方法を以下に述べる．

\subsubsection*{計測方法}
本システムでは，フォトトランジスタと赤外線センサの2種類のセンサを用いて
指揮者の動きを計測する．
フォトトランジスタにより拍のタイミングを検出し，
赤外線センサにより腕の位置や動きを取得することで，
テンポの推定および状態判定を行う．

\subsubsection*{拍の検出（フォトトランジスタ）}
フォトトランジスタは，受光量に応じて電流が変化する素子であり，
アナログピンから出力電圧を継続的に読み取ることで受光量を取得する．
指揮者の腕が振り下ろされた瞬間に点灯するLEDの光を受光することで出力電圧が変化する．
この電圧変化をキャリブレーションにより正規化した値と閾値とを比較することで，
拍の基準となるタイミングを検出する．

また，演奏開始の合図として，正規化後の受光量が\texttt{PERFORM\_START\_THRESH\_MIN}から\texttt{PERFORM\_START\_THRESH\_MAX}
の間の値をとった瞬間を演奏開始の合図として用いる．

\subsubsection*{腕の位置検出（赤外線センサ）}
赤外線センサは，赤外線LEDから照射した光が対象物に反射して返ってくる強度を
フォトトランジスタで受光し，その強度から距離を推定する素子である．
反射強度は距離の増加に伴い減少するため，出力電圧$V$から距離$d$ [cm] を
以下の近似式によって算出する．

\begin{equation}
  d = 27.728 \times V^{-1.2045}
\end{equation}

赤外線センサは腕までの距離を一定周期で取得し，
腕の位置や動きの変化を連続的に観測する．
腕が振り下ろされた状態では距離が最小となるため，
距離の局所的な最大値（ピーク）を検出することで腕の最下点通過を検知する．

\subsubsection*{キャリブレーション}
各楽器ユニット間でセンサ値のスケールを統一するため，
演奏開始前のキャリブレーション期間中に赤外線センサおよびフォトトランジスタの
最大値$d_{\max}$・$p_{\max}$および最小値$d_{\min}$・$p_{\min}$を記録し，
以下の式によって正規化する．

\begin{equation}
  v_{\text{norm}} = \frac{v - v_{\min}}{v_{\max} - v_{\min}} \times 100 \quad [\%]
\end{equation}

これにより，静止判定および拍検出の閾値を全楽器ユニットで統一することができる．
キャリブレーションは電源投入後の最初の1回のみ実施し，
2回目以降はキャリブレーション済みフラグ（\texttt{isCalibrated}）によりスキップする．

\subsubsection*{腕の静止判定}
腕が静止しているかどうかは，
正規化済み距離の移動平均値の変化量が閾値以下である状態が
一定回数連続しているかどうかで判定する．

さらに，腕が振りの最下点や最上点において一時的に速度が低下することによる
誤検出を防ぐため，静止状態と判定するには，
一定時間以上連続して静止条件を満たすことを必要とする．
これにより，瞬間的な停止とフェルマータ状態を区別する．

\subsubsection*{テンポ推定}
本システムでは，フォトトランジスタによるLED検出と赤外線センサによる
最下点予測の2つの方法を組み合わせてテンポを推定する．

フォトトランジスタによって取得した連続する2拍の検出時刻から
拍間隔$\Delta t_n$を求め，指数移動平均（Exponential Moving Average; EMA）
によって拍間隔推定値$T_n$を更新する．

\begin{equation}
  T_n = \alpha \cdot \Delta t_n + (1 - \alpha) \cdot T_{n-1}
\end{equation}

ここで$\alpha$はEMA係数（$0 < \alpha < 1$）であり，
値が大きいほど最新の観測値を重視する．
通常演奏時は$\alpha = 0.2$とし，過去の履歴を重視することで
急激な変動を抑えた滑らかなテンポ追従を実現する．

次の拍の予測時刻$t_{\text{next}}$は以下の式で計算する．

\begin{equation}
  t_{\text{next}} = t_{\text{beat}} + T_n
\end{equation}

EMAによる予測時刻$t_{\text{next}}$と赤外線センサによる最下点到達予測時刻
$t_{\text{predict}}$を比較し，誤差率が閾値$\delta$を超えた場合に
テンポ変化と判断する．

\begin{equation}
  \text{誤差率} = \frac{|t_{\text{next}} - t_{\text{predict}}|}{T_n} \times 100 \quad [\%]
\end{equation}

誤差率が$\delta$（デフォルト10\%）を超えた場合はテンポ変化状態に移行し，
赤外線センサの予測値を優先して採用する．
誤差率が$\delta$以内の場合は通常演奏状態を継続し，
EMAによる予測値を採用する．

本システムでは，指揮者の予備振りとして4回の拍を観測した後に，
各楽器ユニットは演奏を開始する．
また，各楽器はカラーセンサで親機からのカラーキューを受信したタイミングで演奏を開始する．

\begin{table}[h]
\centering
\caption{指揮者の振り回数と各楽器の動作例（カラーキュー受信で開始）}
\begin{tabular}{c|c|c|c|c}
\hline
振り回数 & 指揮者 & 楽器1 & 楽器2 & 楽器3 \\
\hline
0 & 静止 & 停止 & 停止 & 停止 \\
1 & 振り開始 & 予備振り & 予備振り & 予備振り \\
2 & 振り & 予備振り & 予備振り & 予備振り \\
3 & 振り & 予備振り & 予備振り & 予備振り \\
4 & 振り（カラーキュー：楽器1） & 演奏開始 & 予備振り & 予備振り \\
5 & 振り（カラーキュー：楽器2） & 演奏中 & 演奏開始 & 予備振り \\
6 & 振り（カラーキュー：楽器3） & 演奏中 & 演奏中 & 演奏開始 \\
7 & 振り & 演奏中 & 演奏中 & 演奏中 \\
\hline
\end{tabular}
\end{table}

\subsection{状態遷移図}

本システムでは，演奏状態を複数の状態に分けて管理する．

\begin{itemize}
    \item 停止状態（IDLE）：演奏を行わない状態．電源投入時はこの状態から開始する．
  \item キャリブレーション状態（CALIBRATE）：センサの最大・最小値を記録する状態．初回のみ実施する．
  \item 予備振り状態（PREBEAT）：テンポを確定するために拍を観測する状態．
  \item 通常演奏状態（PLAYING）：EMAによる予測値に基づいて演奏を行う状態．
  \item テンポ変化状態（TEMPO\_CHANGE）：赤外線センサの予測値に基づいてテンポ変化に追従する状態．
  \item フェルマータ状態（FERMATA）：音を伸ばす状態．
\end{itemize}

図\ref{fig:state} に状態遷移関係を示す．

\begin{figure}[H]
\centering
\includegraphics[width=0.8\linewidth]{figures/tantoB/state_fig_B.pdf}
\caption{状態遷移図}
\label{fig:state}
\end{figure}

各状態は，センサーから取得した情報に基づいて以下の条件で遷移する．

\begin{itemize}

\item 停止状態 $\rightarrow$ キャリブレーション状態\\
未キャリブレーションの場合，腕の動きが検出された場合に遷移する．

\item 停止状態 $\rightarrow$ 予備振り状態\\
キャリブレーション済みの場合，腕の動きが検出された場合に遷移する．

\item キャリブレーション状態 $\rightarrow$ 予備振り状態\\
一定時間（\texttt{CALIBRATE\_THRESH}）が経過した場合に遷移する．

\item 予備振り状態 $\rightarrow$ 通常演奏状態\\
拍が4回検出された場合に遷移する．

\item 通常演奏状態 $\rightarrow$ フェルマータ状態\\
次の拍の予測時刻を超過し，かつ距離変化が小さい状態が
一定時間以上継続した場合に遷移する．

\item 通常演奏状態 $\rightarrow$ テンポ変化状態\\
EMAによる予測時刻と赤外線センサによる予測時刻の誤差率が
閾値$\delta$を超えた場合に遷移する．

\item テンポ変化状態 $\rightarrow$ 通常演奏状態\\
誤差率が閾値$\delta$以内の状態が3回連続した場合に遷移する．

\item フェルマータ状態 $\rightarrow$ 通常演奏状態\\
腕の動きが再び検出された場合に遷移する．

\item 通常演奏状態・テンポ変化状態 $\rightarrow$ 停止状態\\
最後の拍検出から推定拍間隔の3倍以上の時間が経過した場合に遷移する．

\end{itemize}